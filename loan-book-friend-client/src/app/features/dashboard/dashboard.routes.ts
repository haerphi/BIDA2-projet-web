import { Routes } from '@angular/router';
import { DashboardIndexPage } from './pages/dashboard-index-page/dashboard-index-page';

export const routes: Routes = [
    {
        path: '',
        component: DashboardIndexPage,
    },
];
